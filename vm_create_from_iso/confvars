#!/bin/bash

# LANG=us
LANG=fr

#----------------------------------------------------------------------------
# NAME=buster_${LANG}
# TYPE=buster
# ISO=${HOME}/Bureau/archives/iso/debian-testing-amd64-xfce-CD-1.iso
#----------------------------------------------------------------------------

#----------------------------------------------------------------------------
# NAME=stretch_${LANG}
# TYPE=stretch
# ISO=${HOME}/Bureau/archives/iso/debian-9.9.0-amd64-xfce-CD-1.iso
#----------------------------------------------------------------------------

#----------------------------------------------------------------------------
# NAME=ubuntu-18.04_${LANG}
# TYPE=ubuntu
# ISO=${HOME}/Bureau/archives/iso/ubuntu-18.04.2-live-server-amd64.iso
#----------------------------------------------------------------------------

#----------------------------------------------------------------------------
# NAME=ubuntu-19.04_${LANG}
# TYPE=ubuntu
# ISO=${HOME}/Bureau/archives/iso/ubuntu-19.04-live-server-amd64.iso
#----------------------------------------------------------------------------

#----------------------------------------------------------------------------
# NAME=redhat8_${LANG}
# TYPE=redhat8
# ISO=${HOME}/Bureau/archives/iso/rhel-8.0-x86_64-dvd.iso
#----------------------------------------------------------------------------

#----------------------------------------------------------------------------
# NAME=fedora30_${LANG}
# TYPE=fedora30
# ISO=${HOME}/Bureau/archives/iso/Fedora-Server-dvd-x86_64-30-1.2.iso
#----------------------------------------------------------------------------

#----------------------------------------------------------------------------
# NAME=centos8_${LANG}
# TYPE=centos8
# ISO=${HOME}/Bureau/archives/iso/
#----------------------------------------------------------------------------

#----------------------------------------------------------------------------
# NAME=opensuse15_${LANG}
# TYPE=opensuse
# ISO=${HOME}/Bureau/archives/iso/openSUSE-Leap-15.0-DVD-x86_64.iso
#----------------------------------------------------------------------------

#----------------------------------------------------------------------------
# NAME=opensuseweed_${LANG}
# TYPE=opensuse
# ISO=${HOME}/Bureau/archives/iso/openSUSE-Tumbleweed-DVD-x86_64-Snapshot20190508-Media.iso
#----------------------------------------------------------------------------

#----------------------------------------------------------------------------
# NAME=csr1000v
# TYPE=csr1000v
# ISO=${HOME}/Bureau/archives/iso/csr1000v-universalk9.16.10.01b.iso
#----------------------------------------------------------------------------

#----------------------------------------------------------------------------
# NAME=xrv9k
# TYPE=xrv9k
# ISO=${HOME}/Bureau/archives/iso/xrv9k-fullk9-x.vrr.vga-6.5.3.iso
#----------------------------------------------------------------------------

#----------------------------------------------------------------------------
BULK=${HOME}/cloonix_data/bulk
QCOW2=${BULK}/${NAME}.qcow2
CDROM="-cdrom ${ISO}"
CLOONIX_BIN="/usr/local/bin/cloonix"
QEMU_BIN="${CLOONIX_BIN}/server/qemu/qemu_bin"
#----------------------------------------------------------------------------

